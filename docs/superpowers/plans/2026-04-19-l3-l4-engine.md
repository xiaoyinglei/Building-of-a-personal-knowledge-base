# L3/L4 Engine Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在保持旧查询外壳兼容的前提下，把 L3/L4 主链替换为异步核心 + 同步桥接的新引擎结构。

**Architecture:** `planning_graph` 产出策略，`retrieval_adapter` 执行 branch 编排，`rerank_service` 负责清洗和精排，`runtime_coordinator` 统一同步桥接和遗留返回值兼容。

**Tech Stack:** Python, Pydantic, existing retrieval/runtime stack, async bridge via `asyncio.run`

---

### Task 1: Core Skeleton

**Files:**
- Create: `rag/retrieval/planning_graph.py`
- Create: `rag/retrieval/retrieval_adapter.py`
- Create: `rag/retrieval/rerank_service.py`
- Create: `rag/retrieval/runtime_coordinator.py`

- [ ] Step 1: Keep async interfaces at the core boundary
- [ ] Step 2: Encode predicate planning and semantic routing
- [ ] Step 3: Add pre-rerank cleanup and 50-item hard cap
- [ ] Step 4: Add lean payload -> legacy payload compatibility shim

### Task 2: Runtime Wiring

**Files:**
- Modify: `rag/retrieval/orchestrator.py`
- Modify: `rag/runtime.py`

- [ ] Step 1: Replace direct branch orchestration with `planning_graph` + `retrieval_adapter`
- [ ] Step 2: Move retrieval core to `_async_retrieve_pipeline`
- [ ] Step 3: Keep sync `retrieve()` as compatibility bridge
- [ ] Step 4: Preserve existing diagnostics defaults through shim inflation

### Task 3: Verification

**Files:**
- Create: `tests/service/test_planning_graph.py`
- Create: `tests/service/test_industrial_rerank_service.py`
- Create: `tests/service/test_runtime_coordinator.py`
- Reuse: `tests/service/test_retrieval_service.py`

- [ ] Step 1: Verify planning predicate behavior on large scope
- [ ] Step 2: Verify 50-item hard cap before rerank
- [ ] Step 3: Verify compatibility shim default filling
- [ ] Step 4: Re-run existing retrieval service regression tests

### Task 4: Next Iteration

**Files:**
- Future: `rag/retrieval/retrieval_adapter.py`
- Future: `rag/storage/search_backends/milvus_vector_repo.py`

- [ ] Step 1: Add true Milvus hybrid retriever
- [ ] Step 2: Push `expr` into every `AnnSearchRequest`
- [ ] Step 3: Route UI directly to async retrieval path
